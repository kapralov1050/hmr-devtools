/**
 * Public types barrel пакета `vite-agent-bridge`.
 *
 * Импортируйте типы напрямую из пакета:
 * ```ts
 * import type {Manifest, LogPayload} from 'vite-agent-bridge';
 * ```
 */
export type {
    InstanceId,
    InstanceState,
    Manifest,
    ManifestTool,
    LogPayload,
    DomSpec,
    DomResponse,
    AgentEnvelope,
} from './client/types';
